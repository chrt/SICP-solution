#lang sicp

; When an uncertain number is repeated in an expression, we have no way to specify that they are the same.
; To design an interval-arithmetic package that does not have this shortcoming,
; we need a way to compute the range of a multivariable rational function restricted to an interval.
